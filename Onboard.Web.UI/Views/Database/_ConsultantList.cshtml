@model IEnumerable<Onboard.Web.UI.Models.HRViewModels.CandidateViewModel>

<table class="table table-hover row-selected" id= "Consultant-Lists">
    <tbody>
            @foreach (var candidate in Model)
            {
                <tr pk-id="@candidate.EnrollmentId">
                    @*<td class="project-title">
                        @if (client.Status == "A")
                        {
                            <span class="label label-success">Active</span>
                        }
                        else
                        {
                            <span class="label label-danger">Inactive</span>
                        }
                    </td>*@
                    <td class="project-title">
                        @candidate.ConsultantName
                    </td>
                    
                    <td class="project-actions">
                        <a href="#" class="btn btn-white btn-sm" action="view-client"><i class="fa fa-folder" id="@candidate.CandidateName"></i> View </a>
                    </td>
                </tr>

            }
        </tbody>
</table>